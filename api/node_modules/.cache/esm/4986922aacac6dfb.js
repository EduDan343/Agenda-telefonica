let mongoose;_aca‍.x([["User",()=>User]]);_aca‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const Schema = mongoose.Schema;

const UserSchema = new Schema({
  name: String,
  lastName: String,
  email: String,
  phone: Number,
});

       const User = mongoose.model("User", UserSchema);

// export default User;
